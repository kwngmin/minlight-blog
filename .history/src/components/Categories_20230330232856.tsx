type props = {
  categories: string[];
  selected: string;
  onClick: () => {};
};

export default function Categories({
  categories,
  selected,
  onClick,
  setSelected,
}: props) {
  return <></>;
}
