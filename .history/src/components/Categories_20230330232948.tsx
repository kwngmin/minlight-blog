type props = {
  categories: string[];
  selected: string;
  onClick: (category) => void;
  setSelected: function name(params:type) {
    
  }
};

export default function Categories({
  categories,
  selected,
  onClick,
  setSelected,
}: props) {
  return <></>;
}
